app = "quality-gate-dashboard-gschull"

[build]
  dockerfile = "Dockerfile"

[env]
  INGEST_TOKEN = "change-me"
  DATABASE_URL = "/app/metrics.db"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0