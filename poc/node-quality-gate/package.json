{
  "name": "node-quality-gate-poc",
  "version": "0.1.0",
  "private": true,
  "description": "POC for Quality Gate++: Jest + Stryker + Lighthouse CI",
  "scripts": {
    "test": "jest --ci",
    "mutate": "stryker run",
    "lhci": "lhci autorun --config=./lighthouserc.json",
    "dep:health": "node ./scripts/dep_health.js",
    "quality:ci": "npm run test && npm run mutate && npm run lhci && npm run dep:health"
  },
  "devDependencies": {
    "@lhci/cli": "^0.12.0",
    "@stryker-mutator/core": "^8.2.0",
    "@stryker-mutator/jest-runner": "^8.2.0",
    "jest": "^29.7.0"
  }
}