name: Quality Gate

on:
  pull_request:

jobs:
  quality-gate:
    uses: gschull/software-quality-collapse/.github/workflows/quality-gate-reusable.yml@main
    with:
      py_mutation_min: '90'
      py_dep_health_fail: 'true'
      java_mutation_min: '90'
      java_cvss_threshold: '0.1'
